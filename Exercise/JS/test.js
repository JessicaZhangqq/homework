var message = 'first';

console.log(message.charAt(0).toUpperCase()+message.slice(1,message.length));

java 8
python 3.2
git;  big bucket  ; jacons  :environment management
relation database ;
domain 
hido for unit test 
micro se